/*
Author: Jakob Orel

RAM8: n(size)=8, k(address length)=3
    out=(t)=RAM[address(t)](t)
    If load(t-1) then
        RAM[address(t-1)](t)=in(t-1)
*/

CHIP RAM8{
IN in[16], address[3], load;
OUT out[16];
PARTS:

}
